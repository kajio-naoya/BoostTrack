# Windows-friendly requirements for BoostTrack
# Usage:
#   python -m venv .venv
#   .venv\\Scripts\\pip install --upgrade pip
#   .venv\\Scripts\\pip install -r requirements.txt

# Install PyTorch CUDA 12.1 wheels (keep PyPI as default, add PyTorch index as extra)
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2

# Build tools (needed by some packages like lap, pycocotools)
cython==3.0.2
ninja==1.11.1
wheel==0.41.2
setuptools==68.0.0

# Core dependencies from the original env (Linux-specific conda pins removed)
appdirs==1.4.4
beautifulsoup4==4.12.2
coloredlogs==15.0.1
fs==2.4.16
fsspec==2023.9.2
gdown==4.7.1
humanfriendly==10.0
imageio==2.31.5
joblib==1.3.2
# Windows-friendly LAP replacement (prebuilt wheels)
lapx==0.5.9
lazy-loader==0.3
loguru==0.7.2
oauthlib==3.2.2
# ONNX stack (align with yolox which requires onnx>=1.13.0)
onnx==1.17.0
# onnxoptimizer is optional; omit to avoid version pin conflicts
onnxruntime==1.17.3
opencv-python==4.8.1.78
pandas==2.2.2
protobuf==3.20.3
python-dateutil==2.8.2
pytz==2023.3.post1
pywavelets==1.6.0
scikit-image==0.22.0
scikit-learn==1.5.2
six==1.16.0
soupsieve==2.5
tabulate==0.9.0
termcolor==2.1.0
thop==0.1.1-2209072238
threadpoolctl==3.2.0
tifffile==2023.7.10
tzdata==2023.3
yacs==0.1.8
yolox==0.3.0

# ReID library (pip package)
torchreid==0.2.5

# COCO API
# Prefer official package; builds from source on Windows if wheel not available
pycocotools>=2.0.7

# If you need CPU-only PyTorch instead, replace the index above with:
# --index-url https://download.pytorch.org/whl/cpu

